using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BackgroundFriend : MonoBehaviour
{
    string hi;
    // Start is called before the first frame update
    void Start()
    {
        hi = "hi";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
